import styled from 'styled-components';

const StyledNavbar = styled.section`
    display: flex;
    align-items: center;

    .search-input {
        align-self: center;
    }
`

export default StyledNavbar;